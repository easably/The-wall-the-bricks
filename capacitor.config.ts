import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.englishingames.thewall',
  appName: 'the-wall',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
