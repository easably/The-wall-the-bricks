import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.inglishingames.thewall',
  appName: 'thewall',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
